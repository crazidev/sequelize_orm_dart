Define your models using `@Table` annotations and Dart classes.

The code generator produces type-safe model classes, query builders,
DTOs, column references, and include helpers from your annotated classes.
