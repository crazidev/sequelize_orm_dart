Define relationships between models using `@HasOne`, `@HasMany`,
and `@BelongsTo` annotations.

Associations enable eager loading via the generated `IncludeHelper`
with full support for nested includes, filtering, and ordering.
