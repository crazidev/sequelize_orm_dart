Getting started with `sequelize_orm`.

Add the packages to your `pubspec.yaml`:

```yaml
dependencies:
  sequelize_orm: ^0.1.4

dev_dependencies:
  sequelize_orm_generator: ^0.1.4
  build_runner: ^2.10.4
```

Then run `dart pub get` and annotate your models with `@Table`.
