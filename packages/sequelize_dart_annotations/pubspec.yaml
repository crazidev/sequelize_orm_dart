name: sequelize_dart_annotations
description: Annotations for sequelize_dart code generation. Platform-independent.
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.9.2

dependencies:
  meta: ^1.11.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
